/// Type of note
enum NoteType {
  sermon,
  journal,
}

/// Type of media attachment
enum MediaType {
  image,
  sticker,
}

/// Theme mode for the application
enum AppThemeMode {
  light,
  dark,
  system,
}
